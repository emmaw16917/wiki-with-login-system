wiki-project/
├── backend/                # Go后端项目
├── frontend/               # Vue前端项目
└── docs/                   # 项目文档

backend/
├── cmd/                    # 程序入口
│   └── server/
│       └── main.go         # 主函数入口
├── config/                 # 配置文件
│   ├── app.go              # 应用配置
│   └── database.go         # 数据库配置
├── internal/               # 内部模块
│   ├── api/                # API处理层
│   │   ├── handler/        # 处理器
│   │   │   ├── auth.go     # 认证相关(登录/注册)
│   │   │   └── wiki.go     # Wiki相关接口
│   │   ├── middleware/     # 中间件
│   │   │   ├── auth.go     # 认证中间件
│   │   │   └── cors.go     # 跨域中间件
│   │   └── router/         # 路由定义
│   │       ├── auth.go     # 认证路由
│   │       ├── wiki.go     # Wiki路由
│   │       └── router.go   # 路由汇总
│   ├── model/              # 数据模型
│   │   ├── user.go         # 用户模型
│   │   └── page.go         # Wiki页面模型
│   ├── repository/         # 数据访问层
│   │   ├── user_repo.go    # 用户数据操作
│   │   └── page_repo.go    # 页面数据操作
│   └── service/            # 业务逻辑层
│       ├── auth_service.go # 认证业务
│       └── wiki_service.go # Wiki业务
├── pkg/                    # 公共包
│   ├── database/           # 数据库连接
│   ├── logger/             # 日志工具
│   └── utils/              # 工具函数
│       ├── jwt.go          # JWT工具
│       └── password.go     # 密码加密
├── migrations/             # 数据库迁移文件
│   ├── 001_create_users_table.sql
│   └── 002_create_pages_table.sql
├── go.mod                  # Go模块依赖
└── .env                    # 环境变量配置

frontend/
├── public/                 # 静态资源
│   ├── index.html          # 入口HTML
│   └── favicon.ico         # 网站图标
├── src/
│   ├── assets/             # 资源文件
│   │   ├── css/            # 样式文件
│   │   └── images/         # 图片资源
│   ├── components/         # 公共组件
│   │   ├── common/         # 通用组件
│   │   │   ├── Header.vue
│   │   │   └── Footer.vue
│   │   └── wiki/           # Wiki相关组件
│   │       ├── PageCard.vue
│   │       └── Editor.vue
│   ├── router/             # 路由配置
│   │   └── index.js        # 路由定义
│   ├── store/              # 状态管理
│   │   ├── index.js        #  store入口
│   │   └── modules/
│   │       ├── auth.js     # 认证状态
│   │       └── wiki.js     # Wiki状态
│   ├── services/           # API服务
│   │   ├── api.js          # 基础API配置
│   │   ├── authService.js  # 认证相关API
│   │   └── wikiService.js  # Wiki相关API
│   ├── views/              # 页面视图
│   │   ├── auth/
│   │   │   ├── Login.vue   # 登录页
│   │   │   └── Register.vue # 注册页
│   │   ├── Home.vue        # 首页
│   │   ├── WikiList.vue    # Wiki列表页
│   │   └── WikiDetail.vue  # Wiki详情页
│   ├── utils/              # 工具函数
│   │   ├── auth.js         # 认证工具
│   │   └── format.js       # 格式化工具
│   ├── App.vue             # 根组件
│   └── main.js             # 入口JS
├── package.json            # 依赖配置
└── vue.config.js           # Vue配置

数据库设计(MySQL)
- users 表
	- id, username, password_hash
- pages 表
	- id, title, slug, content,, last_editor_id
